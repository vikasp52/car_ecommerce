import 'package:flutter/material.dart';

class Car {
  String name;
  String image;
  double rating;
  String price;
  String color;
  String carType;
  String brand;
  String description;

  Car({this.name, this.image, this.brand, this.price, this.color, this.carType, this.rating, this.description});
}
